{
  "name": "secure-chat-railway",
  "version": "1.0.0",
  "description": "带管理员审核的加密聊天室 (Railway 专用)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "node migrate.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0",
    "ws": "^8.17.1"
  },
  "engines": {
    "node": "20.x"
  }
  {
  "scripts": {
    "build": "npm run build-frontend && npm run build-backend",
    "build-frontend": "mkdir -p public && cp index.html public/",
    "build-backend": "echo 'Backend build completed'",
    "start": "node server.js"
  }
}
}
